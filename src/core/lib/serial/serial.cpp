#include "serial.h"
#include <memory>

namespace serial {

template <typename T>
class Serializer {
   public:
   private:
};

static void Serialize(data_type type, size_t data) {
    malloc(sizeof(data));
    malloc(sizeof(type));
}
}
