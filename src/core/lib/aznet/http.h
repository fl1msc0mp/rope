#ifndef AZNET_HTTP_H_
#define AZNET_HTTP_H_

#include <vector>

namespace aznet {
namespace {

typedef struct http_req { size_t len; } http_req;

typedef struct http_resp { size_t len; } http_resp;

class HTTPClient {
   public:
   private:
};

class HTTPServer {
   public:
   private:
};
}
}
#endif
