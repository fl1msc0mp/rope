#include "registry.h"

namespace atmoz {
namespace {

Registry::Registry(){};

void Registry::AddNode(Registry* registry) { registry = nullptr; };
void Registry::RemoveNode(Registry* registry) { registry = nullptr; };
}
}
